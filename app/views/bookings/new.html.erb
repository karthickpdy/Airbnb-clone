<h1>New booking</h1>

<% form_for(@booking) do |f| %>
  <%= f.error_messages %>
<%=render :partial =>"formbook",:locals => {:f =>f}%>
<%=f.hidden_field "property_id",:value => @propid%>
  <p>
    <%= f.submit 'Create',:class => "btn btn-primary" %>
  </p>
<% end %>
