<h1>Editing property</h1>

<% form_for(@property) do |f| %>
  <%= f.error_messages %>
  <%= render :partial=> "formprop", :locals => { :f => f}%>
  <p>
    <%= f.submit 'Update' ,:class => "btn btn-primary"%>
  </p>
<% end %>

<%= link_to 'Show', @property %> |
<%= link_to 'Back', properties_path %>